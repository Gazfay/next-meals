import { ShareMeal } from '@/screens';

export const metadata = {
  title: 'Next Food | Share meal',
  description: 'Share meal page',
};

export default function ShareMealPage() {
  return <ShareMeal />;
}
