import { Community } from '@/screens';

export const metadata = {
  title: 'Next Food | Community',
  description: 'Next Food - Community page',
};

export default function CommunityPage() {
  return <Community />;
}
